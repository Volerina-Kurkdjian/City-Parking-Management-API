spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/management_parking_db
    username: admin
    password: "d3v@J$%*28"

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate.format_sql: true
